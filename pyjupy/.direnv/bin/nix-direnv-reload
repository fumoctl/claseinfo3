#!/usr/bin/env bash
set -e
if [[ ! -d "/home/fumoctl/Projects/claseinfo3/pyjupy" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/fumoctl/Projects/claseinfo3/pyjupy")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/fumoctl/Projects/claseinfo3/pyjupy" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/fumoctl/Projects/claseinfo3/pyjupy/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/fumoctl/Projects/claseinfo3/pyjupy/.envrc" "/home/fumoctl/Projects/claseinfo3/pyjupy/.direnv"/*.rc
